{% extends 'index.html.twig' %}

{% block contenido %}

<div id="p" >
    <div class="row">
    <div class="col-12"><h3>{{  producto.nombre }}</h3> </div>
        <div class="col-md-1"></div>
        <div class="col-md-4">
        <img src="{{ asset('fotos/' ~ producto.foto) }}" style="width: 100%"></div>
    </div>
</div>

{% endblock %}